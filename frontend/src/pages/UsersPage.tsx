import { PagePlaceholder } from '../components/common/PagePlaceholder'

export function UsersPage() {
  return (
    <PagePlaceholder
      title="Usuarios y Roles"
      description="Base inicial para administración de accesos y permisos."
    />
  )
}
